/*
 * This file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Java project to get you started.
 * For more details take a look at the Java Quickstart chapter in the Gradle
 * User Manual available at https://docs.gradle.org/5.6.2/userguide/tutorial_java_projects.html
 */

buildscript {
  repositories {
        jcenter()
   }
}

plugins {
    id "com.jfrog.bintray" version "1.8.4"
    id 'maven-publish'
    id 'java'
    id 'application'
}

repositories {
    jcenter()
}

dependencies {
    testImplementation 'junit:junit:4.12'
}

application {
    mainClassName = 'com.android.dx.merge.DexMerger'
}

sourceCompatibility = '1.7'
targetCompatibility = '1.7'

task sourcesJar(type: Jar) {
    from sourceSets.main.allJava
    archiveClassifier = 'sources'
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            groupId = 'org.bladetools'
            artifactId = 'bladedx'
            version = '1.0.2'
            artifact sourcesJar
            from components.java
        }
    }
}

bintray {
	user = project.hasProperty('bintrayUser') ? project.property('bintrayUser') : System.getenv('BINTRAY_USER')
	key = project.hasProperty('bintrayApiKey') ? project.property('bintrayApiKey') : System.getenv('BINTRAY_API_KEY')
	publications = ['mavenJava']
	pkg {
		repo = 'bladetools'
		name = 'bladedx'
		userOrg = user
		licenses = ['Apache-2.0']
		vcsUrl = 'https://github.com/bladetools/BladeDx.git'
		labels = ['dx', 'dx.jar', 'blade', 'bladedx']
		publicDownloadNumbers = true
		version {
			name = '1.0.2'
			desc = 'A tool for dex file merging'
		}
	}
}
